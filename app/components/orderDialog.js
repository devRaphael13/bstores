"use client"
import { useEffect, useState } from "react"

export default function OrderDialog() {
    // useEffect(() => {
    //     const orderDialog = document.getElementById("orderDialog")
    //     orderDialog.showDialog()
    // })

    return (
        <h1>Testing dialog</h1>
    )
}